const User = require('./User');
const Pet = require('./Pet');
const Service = require('./Service');
const Appointment = require('./Appointment');

module.exports = { User, Pet, Service, Appointment };
